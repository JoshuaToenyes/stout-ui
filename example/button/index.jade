extends ../_blueprint/master.jade

block content

  h1.ex-title Stout Buttons
  
  .ex-heading Primary Buttons
  p Primary buttons are available in six sizes, <code>tiny</code>, 
    | <code>small</code>, <code>normal</code>, <code>large</code>,
    | <code>huge</code>, and <code>massive</code>. The primary button's 
    | colors are drawn from the primary colors of the configured color scheme.
    | Unless otherwise specified (see below) all buttons feature active, hover,
    | focus, and click events.
  
  #demo
  
  .ex.primary-tiny
    .ex-label tiny
  
  .ex.primary-small
    .ex-label small
      
  .ex.primary-normal
    .ex-label normal  
    
  .ex.primary-large
    .ex-label large

  .ex.primary-huge
    .ex-label huge

  .ex.primary-massive
    .ex-label massive

  button#show Show Button
  button#hide Hide Button
