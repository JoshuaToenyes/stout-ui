extends ../_blueprint/master.jade

block content

  h1.ex-title Stout Modal
  
  .ex-heading Modal Backdrop
  p The modal backdrop isolates the main application window, forcing the user
    | to switch their attention. The backdrop can be used independently via 
    | the <code>modal/backdrop</code> module.
  
  .ex.basic
    .ex-label backdrop
    .controls
  
